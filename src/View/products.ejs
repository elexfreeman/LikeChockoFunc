<div class="content container grid-xl center-container">
    <div class="products">
        <% products.forEach(function(p) { %>
        <div class="cart product">
            <a href="/<%= p.url %>">
                <div class="cart-image">
                    <img class="p-img img-1" src="/img/<%= p.img %>" alt="<%= p.caption %>">
                    <img class="p-img img-2" src="/img/<%= p.bgImg %>" alt="<%= p.caption %>">
                </div>
            </a>
            <div class="cart-header">
                <div class="cart-title h5">
                    <a href="/<%= p.url %>">
                        <%= p.caption %>
                    </a>
                </div>
                <div class="cart-subtitle text-gray">
                    <a href="/<%= p.url %>">
                        <%- p.description %>
                    </a>
                </div>
            </div>
            <div class="cart-body">

                <div class="price">
                    Цена: <b><%= p.price %></b> руб.
                </div>

            </div>
            <div class="cart-footer text-right">
                <div product_img="/img/<%= p.img %>" product_id='<%= p.id %>'
                    product_caption='<%= p.caption %>' 
                    product_price='<%= p.price %>' 
                    class='cart-button'>
                </div>
            </div>

        </div>
        <% }); %>
    </div>

</div>